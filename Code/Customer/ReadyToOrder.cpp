#include "ReadyToOrder.h"
#include "../Floor/Table.h"

/**
 * @brief Executes when customer is ready to order
 * 
 * @param c a pointer to the Customer
 *
 * @return A boolean that represents the Customers readiness to order
 */ 

bool ReadyToOrder::getReadyToOrder(Customer *c) { return true; }

ReadyToOrder::ReadyToOrder() {}
